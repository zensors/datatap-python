from typing import Optional
import tensorflow as tf

def decode_jpeg(img: bytes, channels: Optional[int] = ..., dtype: tf.DType = ..., name: str = ..., expand_animations: bool = ...) -> tf.Tensor: ...